
<article>

	<img src="{{TimberImage(post.get_field('lead_img')).src}}" />
	<div "diary-travel-mod">
		<img src="{{TimberImage(post.get_field('distance_img')).src}}" />
		<img src="{{TimberImage(post.get_field('elevation_img')).src}}" />
		<div "diary-travel-toggle">
			
		</div>
	</div>

	<h1>{{post.post_title}}</h1>
	date/location-location
	<div>{{post.get_content(0, 1)}}</div>

</article>